{
  "buildCommand": "cd simple-preview && npm run build && mkdir -p dist && ls -la && ls -la dist || echo 'dist not found, checking for build output' && find . -name 'index.html' -type f",
  "outputDirectory": "simple-preview/dist",
  "installCommand": "cd simple-preview && npm install",
  "devCommand": "cd simple-preview && npm run dev",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}